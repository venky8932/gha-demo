name: Hello World 
on: 
  push:
    branches: 
      - main
      - develop
      - "release/**"
  pull_request:
    branches: 
      - main
      - develop      

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo Build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test phase
        run: echo "Running test cases"
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Deploy the App
        run: echo "Deploying the app"
      
